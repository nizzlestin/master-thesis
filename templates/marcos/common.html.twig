{% macro graphCard(id, title) %}
    <div class="card">
        <div class="card-header text-center">{{ title }}</div>
        <div id="{{ id }}" class="card-body text-center"></div>
    </div>
{% endmacro %}
