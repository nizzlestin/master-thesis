<form method="post" action="{{ path('app_repo_delete', {'id': repo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repo.id) }}">
    <button class="btn">Delete</button>
</form>
